# This file contains settings for setup.sh that you will need to customize

# Your domain name, or, if a test server, IP address
SERVER_NAME=example.com
# www.SERVER_NAME, or blank if SERVER_NAME is an IP address
ALT_SERVER_NAME=www.example.com

# On DigitalOcean, "ubuntu" user has NOPASSWD privileges in /etc/sudoers.d, so do not use that user.
# Any arbitrary name will do.
DJANGO_USER=du

# Generate your own strong passwords here
DJANGO_USER_PASSWORD=Qh4@*e9Y@7J#
POSTGRES_PASSWORD=A@Thc07Tj4Hx
DJANGO_SUPERUSER_PASSWORD=2yaU!6H*Z2T3

# Email address for the automatically generated superuser
DJANGO_SUPERUSER_EMAIL=myaddress@example.com

#Email address for certbot to send you reminders.  Can be left blank.
CERTBOT_EMAIL_ADDR=myaddress@example.com

# The repo of your Django app
GIT_URL=https://mygithubuser:mygithubtoken@github.com/mygithubuser/myapp.git

# The name of your django app
APP_NAME=myapp

# The location of your STATIC_ROOT directory, relative to the Django root (BASE_DIR)
STATIC_ROOT_RELATIVE=static

# Path to your Django app's pip requirements file
REQUIREMENTS_FILE=requirements/production.txt
